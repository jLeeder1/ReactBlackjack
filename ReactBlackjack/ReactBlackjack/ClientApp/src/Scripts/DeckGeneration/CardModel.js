export default function CreateCardModel(cardNumber, cardSuit, cardValue)
{
    let card = {
        cardNumber: cardNumber,
        cardSuit: cardSuit,
        cardValue: cardValue
    }

    return card;
}
